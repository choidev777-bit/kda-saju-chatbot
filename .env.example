# 사주 챗봇 환경 변수 예시 파일
# 실제 키는 .env 파일에 넣고, .env 는 절대 GitHub 에 올리지 않는다.
# 사용 방법: 이 파일을 복사해 .env 로 만들고 값을 채운다.

# LLM API 키 (둘 중 하나만 있어도 동작. 둘 다 없으면 tool 결과 기반 기본 답변으로 동작)
OPENAI_API_KEY=
GOOGLE_API_KEY=

# 사용할 LLM 제공자 선택: openai 또는 gemini (비워두면 키가 있는 쪽을 자동 선택)
LLM_PROVIDER=
