# 사주 기반 자기성찰 챗봇 PRD

Last Updated: 2026-06-25

## 1. 문서 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | 사주 기반 자기성찰 챗봇 |
| 문서 유형 | PRD, Product Requirements Document |
| 프로젝트 유형 | 초미니 챗봇 프로젝트 |
| 목표 제출물 | PRD 파일, GitHub URL, 기술 스택 목록 |
| 팀원 | 최연준, 이윤서, 최호택, 전원정 |
| MVP 프레임워크 | Streamlit 권장 |
| 핵심 기술 | Python, LangChain `@tool`, LLM API |

## 2. 제품 한 줄 설명

사용자의 생년월일과 출생시간을 바탕으로 사주팔자·오행과 십신·신살·대운 등 명리 요소를 계산하고, 계산된 결과를 LLM이 메뉴별 출력 양식과 상담가 페르소나(날카로운 분석 '팩폭' + 구체적 개운법)로 해석하여 사주풀이, 오늘의 운세, 행운 점수, 행운 색깔, 행운 아이템, 연애운, 재물운, 인생흐름을 제공하는 챗봇이다.

## 3. 문제 정의

사주나 오늘의 운세 서비스는 사용자가 흥미를 느끼기 쉽고 발표 시연에 적합하지만, LLM에게 생년월일만 전달하면 사주 계산을 지어내는 문제가 발생할 수 있다.

이 프로젝트는 다음 문제를 해결한다.

- LLM이 계산을 임의로 생성하는 문제
- 팀원별 tool 구현 역할을 명확히 나누기 어려운 문제
- 운세 답변이 단정적 예측이나 민감한 조언으로 흐를 수 있는 문제
- 과제 조건인 "4가지 이상의 tool"을 자연스럽게 충족해야 하는 문제

## 4. 제품 목표

### 4.1 사용자 목표

- 사용자는 생년월일과 출생시간을 입력해 자신의 사주 기반 성향을 확인할 수 있다.
- 사용자는 오늘의 행운 점수, 행운 색깔, 행운 아이템을 쉽게 확인할 수 있다.
- 사용자는 연애운, 재물운, 인생흐름을 챗봇 대화처럼 자연스럽게 물어볼 수 있다.

### 4.2 프로젝트 목표

- Streamlit 또는 Gradio 기반 챗봇 형태로 구현한다.
- LangChain `@tool` 기반 tool을 4개 이상 구현한다. (현재 6개 구현)
- 팀원 4명이 각각 최소 1개 이상의 tool을 담당한다.
- LLM 환각을 줄이기 위해 계산(만세력·오행·십신·신살·대운 등)은 tool이 수행하고, LLM은 해석만 담당한다.
- 메뉴별 출력 양식과 상담가 페르소나를 LLM 프롬프트에 적용한다.
- PRD, GitHub URL, 기술 스택 목록을 제출 가능한 형태로 준비한다.

## 5. 비목표

이번 MVP에서는 다음을 목표로 하지 않는다.

- 건강운 제공
- 질병, 수명, 사고 예측
- 투자 수익, 주식, 당첨, 합격 여부 예측
- 의학, 법률, 금융 의사결정 조언
- 유파별 정통 기준의 고정밀 대운/용신 보장 (대운·신강신약·용신은 근사값으로 제공)
- 상용 수준의 만세력 정확도 보장
- 로그인, 회원가입, 결제 기능

## 6. 대상 사용자

### 6.1 1차 사용자

운세, 사주, 자기성찰 콘텐츠에 흥미가 있는 일반 사용자

주요 니즈:

- 오늘의 분위기를 가볍게 확인하고 싶다.
- 내 성향을 재미있게 읽어보고 싶다.
- 행운 색깔이나 아이템처럼 바로 써먹을 수 있는 결과를 보고 싶다.

### 6.2 발표 평가자

과제 발표를 보는 선생님과 수강생

주요 니즈:

- 챗봇이 실제로 작동하는지 확인하고 싶다.
- 4개 이상의 tool이 어떻게 쓰였는지 알고 싶다.
- 각 팀원이 어떤 tool을 구현했는지 확인하고 싶다.
- LLM과 tool의 역할 분리가 명확한지 보고 싶다.

## 7. 사용자 시나리오

### 시나리오 1. 첫 이용

1. 사용자가 챗봇에 접속한다.
2. 이름, 성별, 생년월일, 출생시간, 양력/음력 여부를 입력한다.
3. 챗봇이 입력값을 확인한다.
4. 챗봇이 사주 원자료와 오행 분석을 생성한다.
5. 사용자가 메뉴에서 "사주풀이"를 선택한다.
6. 챗봇이 성향, 강점, 주의점을 자연어로 설명한다.

### 시나리오 2. 오늘의 운세 확인

1. 사용자가 기본 정보를 입력한다.
2. 사용자가 "오늘의 운세"를 선택한다.
3. 챗봇이 오늘의 행운 점수를 계산한다.
4. 챗봇이 점수, 근거, 조심할 점을 함께 설명한다.

### 시나리오 3. 행운 색깔/아이템 확인

1. 사용자가 기본 정보를 입력한다.
2. 사용자가 "행운 색깔" 또는 "행운 아이템"을 선택한다.
3. 챗봇이 보완 오행을 기준으로 색깔과 아이템을 추천한다.
4. 챗봇이 추천 이유를 짧게 설명한다.

### 시나리오 4. 연애운/재물운/인생흐름 질문

1. 사용자가 기본 정보를 입력한다.
2. 사용자가 연애운, 재물운, 인생흐름 중 하나를 선택한다.
3. 챗봇이 사주 프로필과 오행 분석 결과를 바탕으로 조언형 답변을 생성한다.
4. 사용자가 후속 질문을 할 수 있다.

## 8. 핵심 기능 요구사항

### FR-1. 사용자 정보 입력

설명:

사용자는 사주 계산에 필요한 기본 정보를 입력할 수 있어야 한다.

입력 항목:

- 이름
- 성별
- 생년월일
- 출생시간
- 양력/음력 여부
- 출생시간 미상 여부

수용 기준:

- [ ] 생년월일은 `YYYY-MM-DD` 형식으로 입력받는다.
- [ ] 출생시간은 `HH:MM` 형식으로 입력받는다.
- [ ] 출생시간을 모르는 경우에도 진행할 수 있다.
- [ ] 필수값이 누락되면 사용자에게 재입력을 요청한다.

### FR-2. 만세력 계산 tool

담당자: 최연준

설명:

사용자 입력을 바탕으로 년주, 월주, 일주, 시주를 계산한다.

Tool:

```python
@tool
def calculate_saju_chart(user_info_json: str) -> str:
    """사용자의 생년월일, 출생시간, 양력/음력 정보를 바탕으로 사주팔자를 계산한다."""
```

수용 기준:

- [ ] LangChain `@tool` 데코레이터를 사용한다.
- [ ] JSON 문자열을 입력받고 JSON 문자열을 반환한다.
- [ ] 결과에 `year_pillar`, `month_pillar`, `day_pillar`, `hour_pillar`가 포함된다.
- [ ] 출생시간 미상 시 `hour_pillar`는 null 또는 제한값으로 처리한다.
- [ ] 계산 실패 시 구조화된 에러 JSON을 반환한다.

### FR-3. 오행 분석 tool

담당자: 이윤서

설명:

사주팔자 결과를 바탕으로 목, 화, 토, 금, 수 오행의 개수와 강약을 분석한다.

Tool:

```python
@tool
def analyze_five_elements(saju_chart_json: str) -> str:
    """사주팔자 데이터를 바탕으로 목, 화, 토, 금, 수 오행의 강약을 분석한다."""
```

수용 기준:

- [ ] 천간/지지 오행 매핑을 코드로 구현한다.
- [ ] 오행별 개수를 반환한다.
- [ ] 강한 오행, 약한 오행, 보완 오행을 반환한다.
- [ ] 동률 상황에서도 안정적으로 결과를 반환한다.

### FR-4. 오늘 운세 점수 tool

담당자: 최호택

설명:

사용자 사주 프로필과 오늘 날짜의 기운을 비교해 오늘의 행운 점수를 계산한다.

Tool:

```python
@tool
def calculate_today_luck(profile_json: str) -> str:
    """사용자 사주 프로필과 오늘 날짜의 기운을 비교해 오늘의 행운 점수를 계산한다."""
```

수용 기준:

- [ ] 점수는 0~100 사이로 반환한다.
- [ ] 기본 점수, 가산점, 감산점 규칙이 코드로 구현된다.
- [ ] 결과에 `score`, `today_element`, `signals`, `cautions`가 포함된다.
- [ ] 점수 근거가 함께 제공된다.

### FR-5. 행운 색깔/아이템 추천 tool

담당자: 전원정

설명:

오행 분석 결과의 보완 오행을 기준으로 행운 색깔과 행운 아이템을 추천한다.

Tool:

```python
@tool
def recommend_lucky_factors(element_analysis_json: str) -> str:
    """보완 오행을 기준으로 행운 색깔과 행운 아이템을 추천한다."""
```

수용 기준:

- [ ] 목, 화, 토, 금, 수 모든 오행에 대해 색깔 매핑이 있다.
- [ ] 목, 화, 토, 금, 수 모든 오행에 대해 아이템 매핑이 있다.
- [ ] 추천 이유를 함께 반환한다.
- [ ] 알 수 없는 오행 입력 시 구조화된 에러 JSON을 반환한다.

### FR-6. 명리 해석 tool

담당자: 최연준 (통합 리드)

설명:

사주팔자(간지)를 바탕으로 십신, 지장간, 신강/신약, 용신, 신살, 대운, 일진을 계산한다. 만세력 라이브러리는 간지·절기 등 1차 재료만 제공하므로, 해석 데이터는 표준 명리 규칙(룩업 테이블)으로 직접 계산한다.

Tool:

```python
@tool
def analyze_myeongri(saju_chart_json: str) -> str:
    """사주팔자로부터 십신·지장간·신강신약·용신·신살을 계산한다."""

@tool
def calculate_iljin(profile_json: str) -> str:
    """출생 일주를 기준으로 오늘(또는 대상 날짜)의 일진 간지를 계산한다."""
```

수용 기준:

- [ ] 십신(천간/지지)과 지장간을 결정적으로 계산한다.
- [ ] 신강/신약과 용신은 근사값임을 결과에 명시한다.
- [ ] 신살(천을귀인·양인·문창·도화·역마·화개·괴강·백호)을 룩업으로 판정한다.
- [ ] 대운은 방향(년간 음양×성별)·대운수·간지 목록을 제공하고, 성별/출생일이 없으면 graceful 처리한다.
- [ ] 일진은 출생 일주 기준 60갑자 순환으로 전 출생연도(1900~2050)를 지원한다.

### FR-7. LLM 해석

설명:

LLM은 tool이 생성한 JSON 결과를 바탕으로, 메뉴별 출력 양식과 상담가 페르소나(팩폭·시적 소제목)에 맞춰 자연어 답변을 제공한다.

수용 기준:

- [ ] LLM은 사주 계산을 직접 하지 않는다.
- [ ] LLM 프롬프트에 "tool 결과만 근거로 해석" 규칙이 포함된다.
- [ ] JSON에 없는 정보는 추측하지 않는다.
- [ ] 답변은 엔터테인먼트 및 자기성찰용 조언으로 제한한다.
- [ ] 건강, 질병, 수명, 사고, 투자 수익을 단정하지 않는다.
- [ ] 메뉴별 지정 출력 양식과 상담가 페르소나를 따른다.

### FR-8. 메뉴별 답변

설명:

사용자는 메뉴 또는 자연어 질문으로 원하는 운세 항목을 선택할 수 있다.

지원 메뉴:

- 사주풀이
- 오늘의 운세
- 오늘의 행운 점수
- 행운 색깔
- 행운 아이템
- 연애운
- 재물운
- 인생흐름

수용 기준:

- [ ] 사용자는 각 메뉴를 선택할 수 있다.
- [ ] 메뉴별로 필요한 tool만 실행된다.
- [ ] 결과는 한국어 자연어 답변으로 제공된다.
- [ ] 메뉴별로 문서에 정의된 출력 양식(만세력 표·시적 소제목·개운법 등)을 따른다.
- [ ] 건강운 요청 시 제외 안내를 제공한다.

## 9. 비기능 요구사항

### NFR-1. 안정성

- 잘못된 입력으로 앱이 중단되지 않아야 한다.
- tool 실패 시 사용자에게 이해 가능한 오류 안내를 제공해야 한다.
- LLM API 실패 시 최소한 tool 결과 기반 기본 메시지를 제공해야 한다.

### NFR-2. 보안

- API 키는 `.env`에 저장한다.
- `.env` 파일은 GitHub에 업로드하지 않는다.
- `.env.example`에는 변수명만 포함한다.
- 발표용 데모에는 가상 인물 데이터를 사용한다.

### NFR-3. 개인정보 보호

- MVP에서는 사용자 정보를 세션 상태에만 저장한다.
- 사용자의 이름과 생년월일을 불필요하게 장기 저장하지 않는다.
- SQLite 또는 JSON 저장을 사용할 경우 저장 목적을 명확히 한다.

### NFR-4. 응답 품질

- 답변은 한국어로 제공한다.
- 과도하게 단정적인 표현을 피한다.
- 계산 근거를 짧게 설명한다.
- 사용자가 이해하기 쉬운 문장으로 작성한다.

### NFR-5. 발표 가능성

- 3분 이내에 주요 기능을 시연할 수 있어야 한다.
- 각 팀원의 tool 기여가 명확히 드러나야 한다.
- GitHub 저장소에서 실행 방법을 확인할 수 있어야 한다.

## 10. 데이터 요구사항

### 10.1 사용자 입력 데이터

```json
{
  "name": "민지",
  "gender": "female",
  "birth_date": "1998-03-12",
  "birth_time": "09:00",
  "calendar_type": "solar",
  "birth_time_unknown": false
}
```

### 10.2 사주 프로필 데이터

```json
{
  "saju_chart": {
    "year_pillar": "무인",
    "month_pillar": "을묘",
    "day_pillar": "병진",
    "hour_pillar": "정사",
    "time_precision": "known"
  },
  "five_elements": {
    "counts": {
      "wood": 3,
      "fire": 2,
      "earth": 2,
      "metal": 0,
      "water": 1
    },
    "strong_element": "wood",
    "weak_element": "metal",
    "recommended_element": "metal"
  },
  "myeongri": {
    "il_gan": "병",
    "sipsin": {
      "year": { "stem": "식신", "branch": "편인" },
      "month": { "stem": "정인", "branch": "정인" },
      "day": { "stem": "일간", "branch": "식신" },
      "hour": { "stem": "겁재", "branch": "비견" }
    },
    "body_strength": { "label": "신강", "score": 6, "method": "weighted_approx" },
    "yongsin": { "favorable_elements": ["earth", "metal", "water"], "method": "eokbu_approx" },
    "sinsal": [
      { "name": "도화", "branch": "묘", "positions": ["month"] }
    ],
    "daewoon": {
      "available": true,
      "direction": "순행",
      "start_age": 8,
      "periods": [
        { "sequence": 1, "age_from": 8, "age_to": 17, "ganji": "병진", "stem_sipsin": "비견" }
      ]
    }
  }
}
```

일진(오늘) 데이터(오늘 운세 메뉴):

```json
{
  "iljin": {
    "date": "2026-06-25",
    "ganji": "무진",
    "stem_sipsin": "식신",
    "branch_sipsin": "식신"
  }
}
```

### 10.3 공통 에러 데이터

```json
{
  "ok": false,
  "error": {
    "code": "INVALID_INPUT",
    "message": "birth_date는 YYYY-MM-DD 형식이어야 합니다."
  }
}
```

## 11. 화면 요구사항

### 11.1 첫 화면

필수 요소:

- 서비스 제목
- 엔터테인먼트/자기성찰 목적 안내문
- 사용자 정보 입력 폼
- 분석 시작 버튼

### 11.2 결과 화면

필수 요소:

- 사용자 사주 기본 정보
- 오행 분석 요약
- 기능 메뉴 버튼 또는 선택 박스
- 챗봇 답변 영역
- 후속 질문 입력창

### 11.3 안내 문구

서비스 화면 또는 결과 하단에 다음 안내를 포함한다.

```text
본 서비스는 전통 명리학 요소를 활용한 엔터테인먼트 및 자기성찰용 챗봇입니다.
의학, 법률, 금융 등 중요한 의사결정의 근거로 사용하지 마세요.
```

## 12. 사용자 흐름

```text
사용자 접속
↓
기본 정보 입력
↓
입력값 검증
↓
만세력 계산 tool 실행
↓
오행 분석 tool 실행
↓
사주 프로필 생성
↓
사용자 메뉴 선택
↓
필요한 tool 실행
↓
LLM 해석 생성
↓
챗봇 답변 출력
```

## 13. 팀원별 책임

| 팀원 | 담당 기능 | 담당 파일 예시 |
|---|---|---|
| 최연준 | 만세력 계산 tool, 명리 해석 tool, 전체 통합 | `src/tools/saju_chart.py`, `src/tools/myeongri.py` |
| 이윤서 | 오행 분석 tool | `src/tools/five_elements.py` |
| 최호택 | 오늘 운세 점수 tool | `src/tools/today_luck.py` |
| 전원정 | 행운 색깔/아이템 추천 tool | `src/tools/lucky_factors.py` |

공동 책임:

- Streamlit/Gradio UI
- LLM 프롬프트
- README
- 발표 자료
- 최종 QA

## 14. 기술 스택 요구사항

과제 제출용 기술 스택 목록에는 다음을 포함한다.

| 구분 | 내용 |
|---|---|
| 프레임워크 | Streamlit 또는 Gradio |
| 언어 | Python |
| LLM 연결 | LangChain |
| Tool 구현 | LangChain `@tool` |
| LLM 모델/API | OpenAI API 또는 Gemini API |
| 만세력 계산 | npm 패키지 `@fullstackfamily/manseryeok`(간지·절기 등 1차 재료) + 십신·신살·대운 등 해석은 자체 명리 룰셋(`src/tools/myeongri.py`, 순수 파이썬) |
| 데이터베이스 | MVP는 없음 또는 session state, 확장 시 SQLite/JSON |
| 주요 라이브러리 | `langchain`, `streamlit`, `python-dotenv`, `pytest`, LLM SDK, 필요 시 `@fullstackfamily/manseryeok` |
| 배포 환경 | Streamlit Community Cloud, Hugging Face Spaces, Render 중 선택 |
| 개발 도구 | GitHub, VS Code, ChatGPT/Codex 등 AI 코딩 도구 |

## 15. 성공 지표

### 기능 성공 지표

- [ ] 6개의 LangChain `@tool` 함수가 구현되어 있다 (4개 이상 요건 충족).
- [ ] 팀원 4명이 각각 1개 이상의 tool을 담당했다.
- [ ] 사용자 입력 후 사주 프로필이 생성된다.
- [ ] 오늘의 행운 점수, 행운 색깔, 행운 아이템이 표시된다.
- [ ] 사주풀이, 연애운, 재물운, 인생흐름 답변이 생성된다.

### 품질 성공 지표

- [ ] LLM이 사주 계산을 직접 하지 않는다.
- [ ] tool 결과 JSON을 근거로 답변한다.
- [ ] 건강운과 의학적 조언을 제공하지 않는다.
- [ ] 잘못된 입력에도 앱이 중단되지 않는다.
- [ ] 발표자가 전체 구조를 설명할 수 있다.

### 제출 성공 지표

- [ ] `PRD.md`가 존재한다.
- [ ] `ARCHITECTURE.md`가 존재한다.
- [ ] `docs/plans/PLAN_saju-chatbot.md`가 존재한다.
- [ ] GitHub URL을 제출할 수 있다.
- [ ] 기술 스택 목록을 제출할 수 있다.

## 16. MVP 범위

MVP에서 반드시 구현할 것:

- 사용자 정보 입력
- 입력 검증
- 만세력 계산 tool
- 오행 분석 tool
- 오늘 운세 점수 tool
- 행운 색깔/아이템 추천 tool
- 명리 해석 tool (십신·신살·대운·일진)
- LLM 해석 (메뉴별 출력 양식 + 상담가 페르소나)
- Streamlit/Gradio UI
- PRD, README, ARCHITECTURE 문서

MVP에서 시간이 남으면 구현할 것:

- 연애운/재물운 전용 분석 (명리 십신 기반으로 구현 완료)
- 대화 기록 저장
- SQLite 저장
- 배포 URL
- 더 정교한 만세력 라이브러리 연동 (`@fullstackfamily/manseryeok` 연동 완료, 명리 해석 레이어 추가)
- `@fullstackfamily/manseryeok` 사용 시 GitHub ZIP 다운로드가 아니라 `npm install @fullstackfamily/manseryeok` 방식으로 설치
- Python `@tool`에서 Node.js helper 스크립트 `scripts/calculate_saju.mjs`를 호출하는 구조 적용

## 17. 리스크 및 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 만세력 계산 구현 난이도가 높다 | 핵심 기능 지연 | MVP 계산 어댑터를 먼저 만들고, 정확도 고도화는 확장으로 둔다. |
| 팀원별 JSON 형식이 다르다 | 통합 실패 | 공통 입력/출력 계약을 문서화하고 테스트로 고정한다. |
| LLM이 환각 답변을 한다 | 신뢰도 하락 | tool 결과만 근거로 답하도록 프롬프트를 제한한다. |
| 건강/투자 관련 민감 답변이 나온다 | 발표 리스크 | 금지 주제를 프롬프트와 UI 안내문에 명시한다. |
| API 키가 노출된다 | 보안 문제 | `.env` 사용, `.gitignore` 관리, `.env.example`만 업로드한다. |
| 발표 직전 실행 오류 | 시연 실패 | 고정 데모 입력값, 예비 스크린샷, 로컬 실행 절차를 준비한다. |

## 18. 완료 기준

프로젝트는 다음 조건을 만족하면 완료로 본다.

- [ ] 사용자가 기본 정보를 입력할 수 있다.
- [ ] 4개 이상의 `@tool`이 실제 코드에 구현되어 있다.
- [ ] 각 팀원이 최소 1개의 tool을 구현했다.
- [ ] LLM이 tool 결과를 바탕으로 답변한다.
- [ ] 건강운은 제외되어 있다.
- [ ] 결과 화면에 사주풀이, 오늘 운세, 행운 색깔/아이템, 연애운, 재물운, 인생흐름 중 MVP 기능이 표시된다.
- [ ] README에 실행 방법이 있다.
- [ ] PRD와 아키텍처 문서가 있다.
- [ ] GitHub 저장소에 코드와 문서가 올라가 있다.

## 19. 오픈 질문

- 최종 UI 프레임워크 → 결정됨: Streamlit.
- 만세력 계산 방식 → 결정됨: `@fullstackfamily/manseryeok` 연동 + 자체 명리 해석 레이어(`src/tools/myeongri.py`).
- LLM API → 결정됨: OpenAI 기본 사용(Gemini도 지원, 키 없으면 tool 결과 기반 fallback).
- 대화 기록 저장을 MVP에 포함할 것인가? (현재 미정 — 세션 상태만 사용)
- 배포를 필수로 할 것인가, 로컬 실행 시연으로 충분한가? (현재 미정)
